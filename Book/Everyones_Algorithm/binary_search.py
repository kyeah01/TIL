# 재귀를 이용한 binary search

def  rec_binary(L, target):
    if not L:
        return
    elif L[len(L)//2] = target:
        return len(L)//2
    elif L[len(L)//2] > target:
          
    elif L[len(L)//2] < target: